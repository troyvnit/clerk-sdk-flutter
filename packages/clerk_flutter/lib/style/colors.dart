import 'package:flutter/material.dart';

/// Reusable color pallette.
class ClerkColors {
  const ClerkColors._();

  /// White.
  static const white = Colors.white;

  /// Grey.
  static const grey = Colors.grey;

  /// Light Grey.
  static final lightGrey = Colors.grey.shade100;

  /// Almost Black.
  static final almostBlack = Colors.black;

  /// Bright Grey (icon and button colors).
  static const brightGrey = Color(0xFF42434d);

  /// Dark Jungle Green (dark text color).
  static const darkJungleGreen = Color(0xFF212126);

  /// White Smoke (divider color).
  static const whiteSmoke = Color(0xFFf5f5f5);

  /// Storm Grey (light text color).
  static const stormGrey = Color(0xFF757686);

  /// Dawn Pink (button edge color).
  static const dawnPink = Color(0xFFebebeb);

  /// Mercury (field border color).
  static const mercury = Color(0xFFe5e5e5);

  /// Nobel (button icon color).
  static const nobel = Color(0xFFb0b0b3);

  /// Charcoal Grey (button gradient first color).
  static const charcoalGrey = Color(0xFF3c3c3d);

  /// Gunmetal (button gradient second color).
  static const gunmetal = Color(0xFF313239);

  /// Seashell (bottom portion divider color).
  static const seashell = Color(0xFFf0f0f1);

  /// Mid Grey (user button title color).
  static const midGrey = Color(0xFF5e5f6e);

  /// Mountain Mist (user button subtitle color).
  static const mountainMist = Color(0xFF9394a1);
}
